nelmio_api_doc:
    documentation:
        info:
            title: PrijsWacht API
            description: API voor het volgen van productprijzen
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc)
