framework:
    rate_limiter:
        domain_scraper:
            policy: sliding_window
            limit: 10
            interval: '1 hour'

        validate_endpoint:
            policy: sliding_window
            limit: 10
            interval: '1 minute'

        check_all_user:
            policy: fixed_window
            limit: 1
            interval: '15 minutes'
